targets:
  $default:
    builders:
      drift_dev:
        options:
          # DateTime come testo ISO-8601 (precisione ai microsecondi, sync-friendly)
          store_date_time_values_as_text: true
